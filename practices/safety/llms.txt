# Safety Best Practices

> Proven patterns for read-only-first workflows, permissions gates, and protective hooks.

## Entries

- [Read-Only First Rule (RULE 0)](read-only-first-rule.md): Gather information before taking action. Read-only commands first. Modifications only with user approval. FOUNDATIONAL.
- [Permissions Pass Gate (RULE 2)](permissions-gate.md): Cryptographic verification gate for privileged Graph API permission changes. RECOMMENDED.
- [tenant-scope-ok Escape Hatch for Multi-Tenant Lint Checks](tenant-scope-ok-escape-hatch.md): Grep-able, audit-friendly escape hatch for the few routes where strict tenant-scope enforcement has a legitimate exception. RECOMMENDED.
- [Versioned Config Migration with Pre-Migration Backup](versioned-config-migration-backup.md): Schema-versioned settings files migrate on load with a version-stamped backup written first and the result saved immediately; retired fields stay deserializable. RECOMMENDED.
- [Secretless Third-Party Writes for Backend-less Clients](secretless-third-party-writes.md): Clients with no server (desktop apps, CLIs, extensions) hand long-form content to the clipboard and open the third party's own creation UI with only short fields prefilled, so the user's own authenticated session performs the write instead of an embedded credential. RECOMMENDED.
